---
- name: Development environment setup
  hosts: localhost
  connection: local
  vars_files:
    - vars.yml

  tasks:
    # ── System packages ────────────────────────────────────────────────────────

    - name: Install apt packages
      become: true
      ansible.builtin.apt:
        name: "{{ apt_packages }}"
        state: present
        update_cache: true
      when: ansible_pkg_mgr == "apt"
      tags: packages

    - name: Install brew packages
      community.general.homebrew:
        name: "{{ brew_packages }}"
        state: present
      when: ansible_pkg_mgr == "brew"
      tags: packages

    # ── User tools (GitHub releases) ───────────────────────────────────────────

    - name: Ensure ~/.local/bin exists
      ansible.builtin.file:
        path: "{{ local_bin }}"
        state: directory
        mode: "0755"
      tags: tools

    - name: Get installed versions of GitHub release tools
      ansible.builtin.shell:
        cmd: "{{ local_bin }}/{{ item.binary }} --version 2>/dev/null | grep -oP '\\d+\\.\\d+\\.\\d+' | head -1 || echo none"
      loop: "{{ github_releases }}"
      register: gh_tool_versions
      changed_when: false
      tags: tools

    - name: Create temp dir for GitHub release tools
      ansible.builtin.tempfile:
        state: directory
        prefix: "gh_tool_"
      register: gh_tmpdir
      tags: tools

    - name: Download and extract GitHub release tools
      ansible.builtin.unarchive:
        src: "https://github.com/{{ item.item.repo }}/releases/download/{{ item.item.tag_prefix | default('v') }}{{ item.item.version }}/{{ item.item.tarball }}"
        dest: "{{ gh_tmpdir.path | default('/tmp') }}"
        remote_src: true
      loop: "{{ gh_tool_versions.results }}"
      when: item.stdout != item.item.version
      tags: tools

    - name: Install GitHub release binaries
      ansible.builtin.shell:
        cmd: "find {{ gh_tmpdir.path | default('/tmp') }} -name {{ item.item.binary }} -type f -exec cp {} {{ local_bin }}/{{ item.item.binary }} \\; && chmod 755 {{ local_bin }}/{{ item.item.binary }}"
      loop: "{{ gh_tool_versions.results }}"
      when: item.stdout != item.item.version
      changed_when: true
      tags: tools

    - name: Clean up temp dir
      ansible.builtin.file:
        path: "{{ gh_tmpdir.path }}"
        state: absent
      tags: tools

    # ── Kitty ──────────────────────────────────────────────────────────────────

    - name: Check if kitty is installed
      ansible.builtin.stat:
        path: "{{ ansible_env.HOME }}/.local/kitty.app/bin/kitty"
      register: kitty_stat
      tags: tools

    - name: Install kitty
      ansible.builtin.shell:
        cmd: "curl -L {{ kitty_install_url }} | sh /dev/stdin launch=n"
      when: not kitty_stat.stat.exists
      tags: tools

    - name: Symlink kitty into ~/.local/bin
      ansible.builtin.file:
        src: "{{ ansible_env.HOME }}/.local/kitty.app/bin/{{ item }}"
        dest: "{{ local_bin }}/{{ item }}"
        state: link
      loop:
        - kitty
        - kitten
      tags: tools

    # ── NVM ────────────────────────────────────────────────────────────────────

    - name: Check if nvm is installed
      ansible.builtin.stat:
        path: "{{ ansible_env.HOME }}/.nvm/nvm.sh"
      register: nvm_stat
      tags: tools

    - name: Install nvm
      ansible.builtin.shell:
        cmd: >
          curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.3/install.sh | bash
      when: not nvm_stat.stat.exists
      tags: tools

    # ── Neovim (from source) ──────────────────────────────────────────────────

    - name: Clone neovim
      ansible.builtin.git:
        repo: "{{ neovim_repo }}"
        dest: "{{ neovim_src }}"
        version: "{{ neovim_version }}"
        depth: 1
      register: nvim_clone
      tags: neovim

    - name: Build and install neovim
      ansible.builtin.shell:
        cmd: "make CMAKE_BUILD_TYPE=Release CMAKE_INSTALL_PREFIX={{ neovim_prefix }} install"
        chdir: "{{ neovim_src }}"
      when: nvim_clone.changed
      tags: neovim

    - name: Neovim installed
      ansible.builtin.debug:
        msg: "Neovim {{ neovim_version }} installed to {{ neovim_prefix }}/bin/nvim. Set NVIM_LOCAL_DIR to sync config to local disk on pull."
      when: nvim_clone.changed
      tags: neovim

    # ── Dotfile symlinks ───────────────────────────────────────────────────────

    - name: Ensure config directories exist
      ansible.builtin.file:
        path: "{{ ansible_env.HOME }}/{{ item.dest | dirname }}"
        state: directory
        mode: "0755"
      loop: "{{ symlinks }}"
      when: (item.dest | dirname) != ""
      tags: dotfiles

    - name: Symlink dotfiles
      ansible.builtin.file:
        src: "{{ dotfiles_dir }}/{{ item.src }}"
        dest: "{{ ansible_env.HOME }}/{{ item.dest }}"
        state: link
        force: true
        follow: false
      loop: "{{ symlinks }}"
      tags: dotfiles

    - name: Ensure scripts are executable
      ansible.builtin.file:
        path: "{{ dotfiles_dir }}/{{ item.src }}"
        mode: "0755"
      loop: "{{ symlinks }}"
      when: item.dest is search("bin/")
      tags: dotfiles

    # ── Git hooks (local only, never committed) ────────────────────────────────

    - name: Install dotfiles post-merge hook
      ansible.builtin.copy:
        content: |
          #!/bin/bash
          if [[ -n "$NVIM_LOCAL_DIR" && -d "$NVIM_LOCAL_DIR" ]]; then
              rsync -a --delete \
                  "$(git rev-parse --show-toplevel)/nvim/.config/nvim/" \
                  "$NVIM_LOCAL_DIR/"
          fi

          # Neovim binary sync removed — wrapper script at ~/.config/work/nvim-wrapper.sh
          # handles XDG paths and points directly to /repo/$USER/neovim/bin/nvim
        dest: "{{ dotfiles_dir }}/.git/hooks/post-merge"
        mode: "0755"
      tags: dotfiles
